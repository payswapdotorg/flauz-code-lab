# Wave 2 / Worker D delivery provenance

- Worker session: flauz-D-w2 (chat.z.ai agents-tab, GLM-5.3 + Full-Stack), chat 7692aa0c-73c0-4885-9c43-1eab65ce6fdd, dispatched by TL#2 via the w2_relay auto-retry daemon (peak-hours GLM-5.3 menu churn).
- Original work: branch wave2/d-license-security (7 local commits @ 3e7d10b1) in the worker sandbox. Push blocked (platform redacts tokens).
- Transit: worker staged to the sandbox template root; TL harvested via the workspaces files API (rev 63568a14, 25/25 files; sha256 verified 24/24 against the worker's MANIFEST.txt). The staging also re-included Wave-1 A files (the branch base) — only D's own deliverables are committed here; A's content already lives on main.
- Worker report highlights: @anthropic-ai/claude-agent-sdk is PROPRIETARY (cglicenses.json) — ship as on-demand download, never bundled; Open VSX first (MS marketplace prohibited); ffmpeg LGPL-2.1+ + H.264 patent decision required; in-tree copilot ext = MIT but entitlement-coupled (disable as default agent); notice-pipeline automation obligation. Security: provenance-tagged context fragments, driver-side browser gate as authoritative layer, credential vault with references-not-copies, claims/leases for agent interference, hash-chained evidence checkpoints. Includes two Wave-1 ARCHITECTURE-MAPPING corrections (AHP SessionPermissionManager exists; workspace-trust path).
