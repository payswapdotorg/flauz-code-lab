# flauz-code-lab

Wave 1 lab repository — docs + evidence only. No product implementation lives here.

Worker C branch: capability truth-table + six-session simulation (see docs/).

## Transit status (Worker C, Wave 1)

The GitHub PAT was stripped from the work-order URL in transit (`x-access-token:` with empty password →
push rejected by GitHub: "Invalid username or token"). All commits live on the local branch
`wave1/c-capability-matrix` (repo on sandbox disk at `/home/z/work/flauz-code-lab`). When a token is
available:

```
cd /home/z/work/flauz-code-lab
git push -u origin wave1/c-capability-matrix   # with credentials configured
```

A bundle mirror also exists at `/home/z/work/flauz-code-lab-wave1-c.bundle` for `git clone`/`git fetch` recovery.

