# flauz-delivery/f-agent-bridge — Wave 3 Lane F transit staging

This directory is the persisted-volume transit copy of the Lane F delivery
(the sandbox cannot push; a pod recycle wipes everything outside
`/home/z/my-project`).

Contents:

- **repo-relative copies of the full delta** vs the upstream baseline
  `9bf9ae764da438b1234a8243dc9e47173ef58ee7` (56 new-path files:
  `extensions/flauz-agent/`, `extensions/flauz-models/`, `build/flauz/`,
  `product.flauz.json`) — paths preserved.
- **`MANIFEST.txt`** — one `<sha256>  <repo-relative-path>` line per delta
  file (regenerated by `build/flauz/stage-delivery.mjs` after every
  milestone).
- **`REPORT.md`** — the seven-section delivery report (WHAT-BUILT /
  VERIFICATION-RECEIPTS / PRODUCT-KEYS / CONTRACT-DEVIATIONS /
  DECISION-LOG-PROPOSALS / GAPS-AND-SKIPS / U-1-CANARY-CHECKLIST).

The canonical copy of the work lives on branch
`flauz/wave3/agent-bridge` in the Flauz clone
(`/home/z/my-project/Flauz`), exported to
`/home/z/my-project/wave3-f-agent-bridge.bundle`
(range `9bf9ae764da..flauz/wave3/agent-bridge`, refreshed after every
milestone).

To verify this staging:

```sh
cd flauz-delivery/f-agent-bridge
# every line of MANIFEST.txt must match the staged copy
(cd . && sha256sum -c MANIFEST.txt)   # GNU coreutils: paths are relative
```
